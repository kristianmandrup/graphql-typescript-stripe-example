import * as React from "react";
import Form from "./Form";
import { Title } from "../../../ui/Title";

export const Create = () => {
  return (
    <div>
      <Title>Create product</Title>
      <Form />
    </div>
  );
};
